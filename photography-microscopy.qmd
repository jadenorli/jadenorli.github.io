---
title: ""
pagetitle: "Microscopy"
format:
  html:
    page-layout: full
    toc: false
execute:
  echo: false
  warning: false
  message: false
---

<div class="page-banner" role="img" aria-label="Microscopy banner">
<video
autoplay muted loop playsinline>
<source src="videos/microscopy.mp4" type="video/mp4">
</video>

<div class="page-banner-overlay"></div>

<div class="page-banner-inner" markdown="1">
  
# Microscopy {.page-banner-title}
<p class="page-banner-subtitle">parasites • textures • invisible architectures</p>

  </div>
</div>

```{r setup-utf8, include=FALSE}
try(Sys.setlocale("LC_CTYPE", "en_US.UTF-8"), silent = TRUE)
options(encoding = "UTF-8")

knitr::knit_hooks$set(output = function(x, options) {
  Encoding(x) <- "UTF-8"
  enc2utf8(x)
})
```

```{r, results='asis'}
library(here)
library(tidyverse)
library(fs)
library(glue)

source("scripts/render_gallery.R")
```


```{r, results='asis'}
render_gallery(
  category      = "microscope",
  metadata_file = "data/microscope_metadata.csv",
  path_prefix   = "images"
)
```

<div class="gallery-footer">
  <a class="btn btn-primary" href="photography.html">Back to Photography</a>
  <a class="read-more" href="#">Back to top</a>
</div>

<!-- REMOVED: the inline masonry shuffle script block.
     scripts/site.js shuffles any .masonry carrying a data-randomize
     attribute, which render_gallery.R emits. The page-level copy hard-coded
     MODE = 'always' and ignored that attribute, so the gallery was being
     reordered twice on every load. -->

